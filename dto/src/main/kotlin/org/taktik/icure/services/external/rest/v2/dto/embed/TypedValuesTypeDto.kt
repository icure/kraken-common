package org.taktik.icure.services.external.rest.v2.dto.embed

/**
 * Enumerates the possible types for a typed value.
 */
enum class TypedValuesTypeDto {
	BOOLEAN,
	INTEGER,
	DOUBLE,
	STRING,
	DATE,
	CLOB,
	JSON,
}
